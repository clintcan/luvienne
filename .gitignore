/target

# macOS drops these into any directory opened in Finder.
.DS_Store

# Screenshots for sharing. Regenerated by driving the app on a pty, so they are
# output rather than source, and a few hundred KB of JPEG would be in the
# history for good.
/luvienne-*.jpg
/luvienne-*.png
